import type { Metadata } from 'next';
import { Star } from 'lucide-react';
import { PageHeader } from '@/components/PageHeader';
import { SectionTitle } from '@/components/SectionTitle';
export const metadata: Metadata = { title: 'Reviews', description: 'Read customer reviews and testimonials for MA Motors.' };
const reviews = [['A flawless experience from enquiry to collection. The car was exactly as described and beautifully prepared.','James R'],['MA Motors gave a fair price for my part exchange and made the finance process simple and quick.','Aisha K'],['Professional, responsive and genuinely helpful. I would happily buy from them again.','Daniel M'],['The team took time to answer every question and the handover felt premium without being pushy.','Sophie L'],['Great stock, clear communication and no surprises. Highly recommended.','Michael T'],['Sold my car to MA Motors and the payment was secure, fast and exactly as agreed.','Priya S']];
export default function ReviewsPage() { return <main><PageHeader eyebrow="Reviews" title="Customer experiences that matter" copy="Our reputation is built on honest descriptions, careful preparation and service that continues after the keys are handed over." /><section className="section-pad bg-night"><div className="container-shell"><SectionTitle align="center" title="Rated for service, preparation and trust" copy="A selection of customer testimonials from buyers and sellers." /><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{reviews.map(([quote, name]) => <article key={name} className="luxury-panel rounded-sm p-6"><div className="flex gap-1 text-gold">{Array.from({ length: 5 }).map((_, index) => <Star key={index} size={18} fill="currentColor" />)}</div><p className="mt-5 leading-7 text-white/72">“{quote}”</p><p className="mt-5 text-sm font-black uppercase tracking-[0.22em] text-gold">{name}</p></article>)}</div></div></section></main>; }
