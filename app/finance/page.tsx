import type { Metadata } from 'next';
import { FinanceCalculator } from '@/components/FinanceCalculator';
import { PageHeader } from '@/components/PageHeader';
import { SectionTitle } from '@/components/SectionTitle';
export const metadata: Metadata = { title: 'Car Finance', description: 'Explore car finance options and example repayments at MA Motors.' };
export default function FinancePage() { return <main><PageHeader eyebrow="Finance" title="Flexible finance for premium used cars" copy="Choose a deposit and term that works for you. Our finance calculator gives a quick indicative repayment before a full quote." /><section className="section-pad bg-night"><div className="container-shell grid gap-10 lg:grid-cols-[.95fr_1.05fr] lg:items-start"><div><SectionTitle title="Representative examples" copy="Finance is subject to status, affordability and lender approval. Final figures depend on vehicle, deposit, term and credit profile." /><div className="mt-8 grid gap-4">{[['£25,000 vehicle','£2,500 deposit','From £563/month over 48 months'],['£45,000 vehicle','£4,500 deposit','From £1,014/month over 48 months'],['£65,000 vehicle','£6,500 deposit','From £1,464/month over 48 months']].map((row) => <div key={row[0]} className="grid gap-3 rounded-sm border border-white/10 bg-white/[.035] p-5 text-sm text-white/68 md:grid-cols-3">{row.map((item) => <span key={item}>{item}</span>)}</div>)}</div></div><FinanceCalculator initialPrice={45000} /></div></section></main>; }
