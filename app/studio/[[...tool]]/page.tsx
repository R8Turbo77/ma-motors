import { StudioClient } from './StudioClient';

export const dynamic = 'force-dynamic';
export const metadata = {
  title: 'MA Motors Admin Studio'
};

export default function StudioPage() {
  return <StudioClient />;
}
